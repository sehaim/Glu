server:
  port: 8083

spring:
  application:
    name: glu-problem
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:test}
  # Cloud
  cloud:
    config:
      enabled: false

  data:
    mongodb:
      uri: mongodb+srv://S11P22A506:denr23bdoE@ssafy.ngivl.mongodb.net/S11P22A506?authSource=admin