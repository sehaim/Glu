server:
  port: 8083

spring:
  application:
    name: glu-problem
  # Cloud
  cloud:
    config:
      enabled: false
  data:
    mongodb:
      host: localhost
      port: 27017

  mongodb:
    embedded:
      version: 4.0.21
