spring:
  application:
    name: glu-auth
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  # Redis
  data:
    redis:
      host: localhost
      port: 6379

  jwt:
    secret: aklsdjlkasjdlkjaskldjklasjdkljaslkdjaslkdjlk

  # ----- Config Server에서 처리 -----
  # Cloud
  cloud:
    config:
      enabled: false

  # MySQL
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3307/glu
    username: root
    password: ssafy

  # JPA
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none

#1초 1000, 1시간 3600000 1달 2592000000
jwt:
  access:
    expiration: 3600000
  refresh:
    expiration: 2592000000